// Simple DatePicker replacement to avoid Radix UI Popover issues\nimport React from 'react';\nimport styled from 'styled-components';\nimport { format } from 'date-fns';\n\nconst DateInput = styled.input`\n  width: 100%;\n  padding: 0.5rem 0.75rem;\n  background-color: #2a2a2a;\n  color: #e0e0e0;\n  border: 1px solid #444;\n  border-radius: 4px;\n  font-size: 0.875rem;\n  \n  &:focus {\n    outline: none;\n    border-color: #e5c76b;\n    box-shadow: 0 0 0 2px rgba(229, 199, 107, 0.2);\n  }\n  \n  &::placeholder {\n    color: #777;\n  }\n`;\n\ninterface SimpleDatePickerProps {\n  date: Date | undefined;\n  onDateChange: (date: Date | undefined) => void;\n  placeholder?: string;\n}\n\nexport const SimpleDatePicker: React.FC<SimpleDatePickerProps> = ({\n  date,\n  onDateChange,\n  placeholder = \"Select date\"\n}) => {\n  const handleChange = (e: React.ChangeEvent<HTMLInputElement>) => {\n    const value = e.target.value;\n    if (value) {\n      onDateChange(new Date(value));\n    } else {\n      onDateChange(undefined);\n    }\n  };\n\n  const dateValue = date ? format(date, 'yyyy-MM-dd') : '';\n\n  return (\n    <DateInput\n      type=\"date\"\n      value={dateValue}\n      onChange={handleChange}\n      placeholder={placeholder}\n    />\n  );\n};\n\n// Export the simple date picker as the default\nexport { SimpleDatePicker as DatePicker };\n