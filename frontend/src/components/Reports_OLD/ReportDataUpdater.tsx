// THIS COMPONENT IS INTENTIONALLY DISABLED TO PREVENT INFINITE LOOPS.
// It should have been removed from all imports and usage.
// If you see errors originating from here, it means it's still being rendered somewhere.
import React from 'react';

console.warn('🚨 CRITICAL WARNING: DISABLED component (ReportDataUpdater) is still being rendered!');

const DisabledComponentStub: React.FC = () => null;

export default DisabledComponentStub;