// Quick password hash verification script\nimport bcrypt from 'bcryptjs';\n\nconst plainPassword = 'Demo123!';\nconst storedHash = '$2a$12$LQv3c1yqBwEHXKn4B8iXKeBQ6JWfq2ZOdtWqWIGU8QGNzAg1v1uCa';\n\nconsole.log('Testing password hash...');\nconsole.log('Plain password:', plainPassword);\nconsole.log('Stored hash:', storedHash);\n\nbcrypt.compare(plainPassword, storedHash)\n  .then(result => {\n    console.log('Password match result:', result);\n    if (result) {\n      console.log('✅ Password hash is correct!');\n    } else {\n      console.log('❌ Password hash is WRONG! Need to regenerate.');\n      // Generate new hash\n      return bcrypt.hash(plainPassword, 12);\n    }\n  })\n  .then(newHash => {\n    if (newHash) {\n      console.log('New correct hash:', newHash);\n    }\n  })\n  .catch(err => {\n    console.error('Error testing password:', err);\n  });\n